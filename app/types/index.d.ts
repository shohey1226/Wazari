//
export enum KeyMode {
  // No keymap applied
  Direct = "direct",

  // Only input keymap
  Terminal = "terminal",

  // Both browser and input keymap
  Text = "text",

  //  Only browser keymap
  Browser = "browser"
}
